let slotTimeCollection = require('../utility/dbconection')

let slotTimeModel = {}

slotTimeModel.addSlotEntry = async (entryInfo) => {
    
};

slotTimeModel.getSlotByID = async (ID) => {
    //Vehicle, user, slot, lot, date
}

